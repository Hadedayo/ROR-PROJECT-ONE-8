<%= form_with(model: customer, local: true) do |form| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, :onfocus => "myFunction(this)", :class => "form-control" %>

  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, :onfocus => "myFunction(this)", :class => "form-control" %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age, :onfocus => "myFunction(this)", :class => "form-control" %>
  </div>

  <div class="field">
    <%= form.label :network_id %>
    <%= form.text_field :network_id, :onfocus => "myFunction(this)", :class => "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>
  <script>
  function myFunction(x) {
      x.style.background = "lightblue";
  }
  </script>
