<script>
function myFunction(x) {
    x.style.background = "yellow";
}
</script>

<%= form_with(model: network, local: true) do |form| %>
  <% if network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network.errors.count, "error") %> prohibited this network from being saved:</h2>

      <ul>
      <% network.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= form.label :name %>
    <div type="text" onfocus="myFunction(this)">
      <%= form.text_field :name %>
    </div>
  </div>

  <div class="field">
    <%= form.label :coverage %>
    <%= form.text_field :coverage %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
