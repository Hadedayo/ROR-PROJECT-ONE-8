<%= form_with(model: network, local: true) do |form| %>
  <% if network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network.errors.count, "error") %> prohibited this network from being saved:</h2>

      <ul>
      <% network.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= form.label :name %>
    <%= form.text_field :name, :onfocus => "myFunction(this)" %>
  </div>

  <div class="field">
    <%= form.label :coverage %>
    <%= form.text_field :coverage, :onfocus => "myFunction(this)" %>
  </div>



  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, :onfocus => "myFunction(this)" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>

  <script>
  function myFunction(x) {
      x.style.background = "#DAA49A";
  }
  </script>
